name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

env:
  PYTHON_VERSION: "3.12"
  APP_NAME: SerialHub-MAIN

jobs:
  build:
    name: Build ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux-x64
            os: ubuntu-latest
            executable: SerialHub-MAIN
            archive: SerialHub-MAIN-linux-x64.tar.gz
          - platform: windows-x64
            os: windows-latest
            executable: SerialHub-MAIN.exe
            archive: SerialHub-MAIN-windows-x64.zip

    defaults:
      run:
        shell: bash

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]" pyinstaller

      - name: Run Ruff
        run: python -m ruff check src tests

      - name: Run pytest
        run: python -m pytest

      - name: Build executable
        run: |
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            data_sep=";"
            icon_args=(--icon src/serialhub/assets/app.ico)
          else
            data_sep=":"
            icon_args=()
          fi

          python -m PyInstaller \
            --name "${APP_NAME}" \
            --onefile \
            --clean \
            --noconfirm \
            --console \
            "${icon_args[@]}" \
            --collect-all textual \
            --collect-all textual_serve \
            --collect-submodules serial \
            --collect-data serialhub \
            --add-data "src/serialhub/serialhub.tcss${data_sep}serialhub" \
            --add-data "src/serialhub/assets${data_sep}serialhub/assets" \
            src/serialhub/__main__.py

      - name: Stage release package
        run: |
          mkdir -p "package/${APP_NAME}"
          cp "dist/${{ matrix.executable }}" "package/${APP_NAME}/"
          cp README.md LICENSE "package/${APP_NAME}/"
          cp src/serialhub/assets/app.png "package/${APP_NAME}/"
          cp src/serialhub/assets/app.ico "package/${APP_NAME}/"

      - name: Create Linux archive
        if: runner.os == 'Linux'
        run: tar -czf "${{ matrix.archive }}" -C package "${APP_NAME}"

      - name: Create Windows archive
        if: runner.os == 'Windows'
        run: 7z a "${{ matrix.archive }}" ./package/${APP_NAME}/*

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.archive }}
          path: ${{ matrix.archive }}
          if-no-files-found: error

  release:
    name: Publish GitHub release
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Read project version
        id: version
        run: |
          version="$(PYTHONPATH=src python -c 'import serialhub; print(serialhub.__version__.split("+", 1)[0])')"
          echo "version=${version}" >> "$GITHUB_OUTPUT"

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          target_commitish: main
          name: SerialHub v${{ steps.version.outputs.version }}
          body: |
            Automated release from the main branch.

            Includes Linux and Windows standalone builds.
          files: release-assets/*
          generate_release_notes: true
          make_latest: true
